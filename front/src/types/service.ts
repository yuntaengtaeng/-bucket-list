export interface ErrorData {
  message: string;
}

export interface UserInfo {
  nickname: string;
  accessToken: string;
}

export interface CategoryList {
  id: string;
  name: string;
  icon: string;
}
